
/*
import { connect } from 'react-redux'
import FicInput from '../components/FicInput'

const mapStateToProps = state => ({
  fic: state.fic
})

//const mapDispatchToProps = (dispatch, ownProps) => ({
//  onChange: () => {
//    dispatch((ownProps.fic))
//  }
//})


const FicInputConnec = connect(
  mapStateToProps 
  //mapDispatchToProps
)(FicInput)

export default FicInputConnec

*/
