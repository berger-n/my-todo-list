
/*
import { connect } from 'react-redux'
import FicSelec from '../components/FicSelec'

const mapStateToProps = (state, ownProps) => ({
  active: ownProps.fic === state.fic
})

const mapDispatchToProps = (dispatch, ownProps) => ({
  onChange: () => {
    dispatch((ownProps.fic))
  }
})

const FicSelecConnec = connect(
  mapStateToProps,
  mapDispatchToProps
)(FicSelec)

export default FicSelecConnec
*/